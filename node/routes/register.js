const registerRoutes = require('express').Router()
const fetch = require('node-fetch')
const db = require('../db')

registerRoutes.get('/:code', async (req, res) => {
	
})


module.exports = registerRoutes